export class Book {
    constructor(public title: string, public author: string, public type: string, public publisher: string, public date: string, public language: string, public country: string, public pages: number, public hashtag: string, public content: string) { }
    

}
